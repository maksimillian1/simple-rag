apiVersion: "cilium.io/v2"
kind: CiliumNetworkPolicy
metadata:
  name: indexer-netpol
spec:
  endpointSelector:
    matchLabels:
      app: indexer
  egress:
  - toEndpoints:
    - matchLabels:
        io.kubernetes.pod.namespace: kube-system
        k8s-app: kube-dns
    toPorts:
    - ports:
      - port: "53"
        protocol: ANY
  - toEndpoints:
    - matchLabels:
        app: qdrant
        io.kubernetes.pod.namespace: rag-platform
    toPorts:
    - ports:
      - port: "6333"
        protocol: TCP
      - port: "6334"
        protocol: TCP
  - toEndpoints:
    - matchLabels:
        app: tei-embeddings
        io.kubernetes.pod.namespace: rag-platform
    toPorts:
    - ports:
      - port: "80"
        protocol: TCP
  - toFQDNs:
    - matchPattern: "*.sqs.*.amazonaws.com"
    toPorts:
    - ports:
      - port: "443"
        protocol: TCP
