# Tech debt

Changes that need a live cluster to test. Numbers come from `docs/report/executions/`; the
multi-tenancy scope is tracked separately in `adr/0013-single-tenant-isolation-scope-tech-debt.md`.

## Platform

| # | Task | Why | Evidence | How to verify |
| :--- | :--- | :--- | :--- | :--- |
| 1 | Upgrade ArgoCD to the latest release: chart `argo-cd` 7.0.0 (v2.11.2) → 10.9.0 (v3.5.2), `argocd-image-updater` 0.11.x → 1.3.1 (v1.3.0). Both are major upgrades: read the upgrade notes first. Then enable `argocd.argoproj.io/compare-options: ServerSideDiff=true` | self-heal loop at rest: $65.85/month cross-AZ + $9.05/month T3 CPU credits. `ServerSideApply` runs with a client-side diff because the `ServerSideDiff` fix panicked on gitops-engine v0.7.1 | `00-baseline` Floor; `00-baseline/data/argocd-loop-probe-2026-09-04T1830.txt`; comment in `deploy/k8s/apps-applicationset.yaml` | at rest: no repeating "Initiating automated sync" in `argocd-application-controller` logs, `applications PATCH` near 0/s, core cross-AZ near 0.9 GiB/h |
| 2 | Keep `argocd-application-controller` and `argocd-repo-server` in one AZ (pod affinity) | the controller pulls full manifests from repo-server on every comparison (`prometheus-stack` with CRDs is 3.98 MB) | same | cross-AZ bytes on core nodes |
| 3 | Next launch and teardown checks | `scripts/teardown-cluster.sh` rewritten, never run: every launch used to leave 2 × 50 GB Qdrant + 2 × 10 GB monitoring volumes ($75.35 spent 08-01 → 09-11). EKS control-plane logging switched off in Terraform ($100.95/month at rest) | `00-baseline` Floor | after `terraform apply`: no `/aws/eks/<cluster>/cluster` log group; after teardown: `aws ec2 describe-volumes --filters Name=status,Values=available` returns nothing |
| 4 | Revert TEI requests to `cpu 3 / limit 4` | HEAD has 6/8 from the r1000 test (`1ef1f0a`); the CPU limit was not the scaling lever, and a 6-CPU request rules out xlarge serving nodes | `02-inference` Saturation; `00-baseline` Configuration freeze (3/4) | r1000 converges to the same p95 (~2425 ms) |
| 5 | Right-size the floor, HA topology unchanged: database 2 × c7g.large, serving 2 × c7i-flex.xlarge (pin `instance-size`), Qdrant PVC 10 Gi, Karpenter requests 250m | fixed floor $866.79 → $779.54/month by sizing alone (serving On-Demand per #6), and → $753.26/month once #12 removes the unreachable endpoint | `00-baseline` Right-sized floor | Qdrant CPU and p95 at r1000 on c7g.large; TEI + API fit one xlarge node; Karpenter healthy at 250m |
| 6 | HA serving base: 2 On-Demand `apps-serving` nodes in different AZs, each running at least 1 API and 1 TEI; scale-out above that stays on Spot | today the pool allows both capacity types with no base, and both floor nodes ran on Spot (a reclaim can take both TEI replicas) | `00-baseline` Floor (2 × c7i-flex.2xlarge Spot at rest) | a second NodePool `apps-serving-base` (`capacity-type: on-demand`, higher `weight`, `limits.cpu` for 2 nodes); TEI `topologySpreadConstraints` on `kubernetes.io/hostname` and `topology.kubernetes.io/zone` (`maxSkew: 1`, `DoNotSchedule`); API keeps its hostname anti-affinity and gets pod affinity to TEI. Check: at rest `kubectl get nodes -L karpenter.sh/capacity-type,topology.kubernetes.io/zone` shows 2 on-demand serving nodes in 2 AZs, each with 1 API + 1 TEI; cost 2 × c7i-flex.xlarge On-Demand = $282.51/month (2xlarge: $565.02) |
| 7 | Split `tei-embeddings` into an ingest-facing and an inference-facing tier, each on its own NodePool. The ingest tier scales on SQS `stage-2` queue depth (the same trigger `indexer` already uses) and is allowed to scale to zero; the inference tier keeps today's CPU-rate trigger and its `minReplicaCount: 2` | one Deployment serves both paths behind one CPU-rate scaler, so the ingest path's producer and consumer scale on different signals: `indexer` ramps to 10 jobs off queue depth at a 15s poll, while TEI needs ~2 min of sustained CPU before it reacts, and TEI is structurally a metric-window behind the work already queued. The shared pool also forces query traffic and batch traffic onto the same nodes, and `minReplicaCount: 2` keeps the ingest side in the floor even when nothing is ingesting | `deploy/k8s/platform/tei-embeddings/scaledobject.yaml`; `deploy/k8s/apps/indexer/scaledjob.yaml` (`aws-sqs-queue`, `queueLength: 5`); `01-ingestion` §3; `00-baseline` Floor (`apps-serving` is the largest B line, $279.82 of $552.91 fixed) | ingest tier reaches zero replicas between batches; at a fixed N the ingest tier's replica count tracks queue depth rather than lagging it; `02-inference` p95 at r1000 unchanged with the query tier isolated |
| 11 | Write `terraform/budgets.tf`: an `aws_sns_topic` with an email subscription plus an `aws_budgets_budget` at 80% actual and 100% forecast, threshold $640.22/month (right-sized Block B × 1.4). The only item here that needs no live cluster: budgets are account-level | nothing watches spend today — no budget, SNS topic or alerting exists anywhere in `terraform/`. `00-baseline` found $175.85/month recurring and $78.31 already spent in errors of exactly the class a spend alarm catches and no test would have | `00-baseline` Floor (errors table); `report.md` §5 Budget alarm | after `terraform apply`: the budget shows under Billing → Budgets, and the SNS email subscription is confirmed from the address it was sent to |

| 12 | Delete the `bedrock` service from `module.vpc_endpoints`, and align the `bedrock-runtime` endpoint with the region the client actually calls — either a model served in `eu-central-1`, or the endpoint moved to the invoked region. Then narrow the Cilium `matchPattern` from `bedrock-runtime.*.amazonaws.com` to that one region | the control-plane endpoint costs $26.28/month and nothing can reach it: `apps/api/go.mod` has no control-plane SDK, IAM grants only `InvokeModel*`, and the egress policy admits only `bedrock-runtime.*`. The runtime endpoint costs the same again and is in `eu-central-1` while `AWS_BEDROCK_REGION` is `us-east-1`, so its private DNS never matches the hostname resolved — generation traffic would leave through NAT, which is the transport `ADR-0007` forbids. The wildcard in the network policy is what lets that pass silently | `terraform/modules/01-rag-core/modules/eks/vpc.tf`; `deploy/k8s/apps/api/deployment.yaml:52-55`; `deploy/k8s/apps/api/network-policy.yaml:48`; `adr/0007-llm-selection-aws-bedrock.md`; report §4.5; `figures.yaml` → `vpc_endpoint_control_plane` | `aws ec2 describe-vpc-endpoints` returns one Bedrock endpoint, in the invoked region; a real-generation run shows no NAT bytes attributable to Bedrock |

## Measurements still open

| # | Task | Why | Where |
| :--- | :--- | :--- | :--- |
| 8 | Contention pass: query load with ingestion running | every query-path finding assumes an idle ingestion path | `report.md` Coverage · `02-inference` Contention pass |
| 9 | One run with real Bedrock generation | generation cost (`E18`, ~$0.51/1k queries) is derived, never measured | `report.md` §1 Verdict and §4.2 · `02-inference` §3 |
| 10 | Capture chunker time-weighted concurrency per N | Fargate break-even (`D29`) needs it | `report.md` §4.4 · `01-ingestion` §3 |
